$("#user-form").html("<%= j (render 'new_user_form') %>")

var modalElement = document.querySelector('#user-form');
var instance = M.Modal.init(modalElement, 
    {
        opacity: 0.9,
        endingTop: '5%',
        onCloseEnd: () => {
            instance.destroy();
        }
    }
);

instance.open();

var elems = document.querySelector('#role');
var instances = M.FormSelect.init(elems, {});
