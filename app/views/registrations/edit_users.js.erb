<%# Renders the edit user form %>
$("#user-form").html("<%= j (render 'edit_user_form') %>");

// Creates a modal
let modalElement = document.querySelector('#user-form');
let instance = M.Modal.init(modalElement, 
    {
        opacity: 0.9,
        endingTop: '5%',
        onCloseEnd: () => {
            instance.destroy();
        }
    }
);

// Opens modal
instance.open();

// Initialize selects and text fields
let elems = document.querySelector('#role');
let instances = M.FormSelect.init(elems, {});
M.updateTextFields();
