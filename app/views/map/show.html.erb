<div>
	<input id="red-slider" type="text" data-slider-min="0" data-slider-max="50" data-slider-step="1" data-slider-value="25" data-slider-id="red-data-slider" data-slider-handle="round"/>
	<input id="red-slider-value" type="text" onkeydown="setRedSliderValue(this.value)"/>
	<input id="yellow-slider" type="text" data-slider-min="0" data-slider-max="100" data-slider-step="1" data-slider-value="50" data-slider-id="yellow-data-slider" data-slider-handle="round"/>
	<input id="yellow-slider-value" type="text" onkeydown="setYellowSliderValue(this.value)"/>
	<input id="yellow-slider-max" type"text" onchange="setYellowMax(this.value)"/>
</div>

<div class="row">
	<div class="col-xs-8">
		<div style='width: 100%;'>
			<div id="map" style='width: 100%; height: 400px;'></div>
			<script type="text/javascript" charset="utf-8">
				// meter = mile * 0.00062137

				map = new google.maps.Map(document.getElementById('map'), {
					center: {lat: 41, lng: -73},
					zoom: 8
					});

				yellow_circle = new google.maps.Circle({
					strokeColor: '#FFFF00',
					strokeWeight: 5,
					fillColor: '#FFFF00',
					fillOpacity: '0.1',
					radius: 80467,
					center: {lat: 41, lng: -73},
					map: map
					});
				red_circle = new google.maps.Circle({
					strokeColor: '#FF0000',
					strokeWeight: 5,
					fillColor: '#FF0000',
					fillOpacity: '0.1',
					radius: 40233,
					center: {lat: 41, lng: -73},
					map: map
					});
				
				$('#yellow-slider').slider({
					slide: function(val){
						circle.setRadius(val);
					}
				});
/*
				var handler = Gmaps.build('Google');
				var yellow, green, red;
				handler.buildMap({ internal: {id: 'map'}}, function(){
					var markers = handler.addMarkers([
						{ lat: 43, lng: 3.5},
						{ lat: 45, lng: 4},
						{ lat: 47, lng: 3},
						{ lat: 49, lng: 4},
						{ lat: 51, lng: 3.5}
						],
						{ strokeColor: '#00FF00' });

					green = handler.addCircles(
						[{ lat: 47, lng: 3, radius: 500000}],
						{
							strokeColor: '#00FF00',
							fillColor: '#00FF00',
							fillOpacity: '0.1'
						});
					yellow = handler.addCircles(
						[{ lat: 47, lng: 3, radius: 250000}],
						{
							strokeColor: '#FFFF00',
							fillColor: '#FFFF00',
							fillOpacity: '0.1'
						});
					red = handler.addCircles(
						[{ lat: 47, lng: 3, radius: 100000}],
						{
							strokeColor: '#FF0000',
							fillColor: '#FF0000',
							fillOpacity: '0.1'
						});

					handler.bounds.extendWith(markers);
					handler.fitMapToBounds();
				});
				yellow.setRadius(1000);*/
			</script>
		</div>	  
	</div>
</div>
