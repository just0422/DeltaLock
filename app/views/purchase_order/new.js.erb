$("#assign-purchaseorder").fadeOut(FADE_TIME, function(){
	create_form("purchaseorder");
	$("#purchaseorder-form").html('<%= j ( render 'newpurchaseorder') %>');
	$("#purchaseorder-form").fadeIn(FADE_TIME);
});
