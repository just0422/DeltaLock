<% if @end_users_list.length > 0 %>
	<h1>End Users</h1>
	<div class="">
		<% @end_users_list.each do |entry| %>
			<%= render '/keycodes/lists/list', list: @end_users_list, entry: entry %>
		<% end %>
	</div>
    <div class="list-group">
        <thead class="thead-inverse">
            <tr>
				<% EndUser.column_names.each do |title| %>
					<%= render '/keycodes/tables/header', title:title %>
                <% end %>
            </tr>
        </thead>
        <tbody>
            <% @end_users_list.each do |entry| %>
				<tr data-link="<%= end_user_path(entry["id"]) %>">
					<%= render '/keycodes/tables/body', entryClass: EndUser, entry: entry %>
                </tr>
            <% end %>
        </tbody>
    </div>
<% end %>
