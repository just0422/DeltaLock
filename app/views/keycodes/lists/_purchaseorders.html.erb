<h1>Purchase Orders</h1>
<table class="table all-tables">
	<thead class="thead-inverse">
		<tr>
			<% PurchaseOrder.column_names.each do |title| %>
				<%= render '/keycodes/tables/header', title:title %>
			<% end %>
		</tr>
	</thead>
	<tbody>
		<% @purchase_orders_list.each do |entry| %>
			<tr data-link="<%= purchase_order_path(entry["id"]) %>">
				<%= render '/keycodes/tables/body', entryClass: PurchaseOrder, entry: entry %>
			</tr>
		<% end %>
	</tbody>
</table>
