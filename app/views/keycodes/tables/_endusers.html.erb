<% if @end_users_list.length > 0 %>
    <h1>End Users</h1>
    <table class="table all-tables">
        <thead class="thead-inverse">
            <tr>
				<% EndUser.column_names.each do |title| %>
					<%= render '/keycodes/tables/header', title:title %>
                <% end %>
            </tr>
        </thead>
        <tbody>
            <% @end_users_list.each do |entry| %>
				<tr data-link="<%= end_user_path(entry["id"]) %>">
				<%= render '/keycodes/tables/body', entryClass: EndUser, entry: entry, classString: "enduser" %>
                </tr>
            <% end %>
        </tbody>
    </table>
<% end %>
