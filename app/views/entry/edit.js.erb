$("#entry-form").html("<%= j (render 'entry/forms/edit') %>");
var modalElement = document.querySelector('#entry-form');
var instance = M.Modal.init(modalElement, 
    {
        opacity: 0.9,
        endingTop: '5%',
        onCloseEnd: () => {
            instance.destroy();
        }
    }
);

instance.open();
M.updateTextFields();
