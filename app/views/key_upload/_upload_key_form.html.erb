<%= form_for @key, url: {controller: :key_upload, action: :create},  html: { multipart: true }, remote: true do |f| %>
	<%= f.label :key %>
	<%= f.text_field :key %>
	<%= f.label :keyfile %>
	<%= f.file_field :keyfile %>
	<%= f.submit "Save", class: "btn btn-primary" %>
<% end %>
